import { blogCallout, blogDivider, blogKeyPoints, blogStats } from './blogBlocks';
import { category } from './category';
import { post } from './post';

export const schemaTypes = [post, category, blogCallout, blogKeyPoints, blogStats, blogDivider];
